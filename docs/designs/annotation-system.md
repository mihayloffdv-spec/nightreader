---
status: ACTIVE
---
# CEO Plan: Annotation System (Deep Reading)
Generated by /plan-ceo-review on 2026-04-03
Branch: main | Mode: SCOPE EXPANSION
Repo: NightReader (local)

## Vision

### 10x Check
Transform NightReader from a PDF viewer into a personal knowledge operating system:
- Living knowledge graph connecting highlights across books
- Spaced repetition built into the reading flow
- AI argument maps (thesis → evidence → conclusion)
- Actions become reminders with progress tracking in library

### Platonic Ideal
The perfect reading companion feels like a brilliant friend who read the book before you. Light pencil marks next to important sentences. One perfect question after each chapter. Distilled takeaways after the book. Gentle reminders of commitments weeks later.

Zero friction during reading (passive AI), maximum depth when engaged (active annotation), lasting impact after the book (export + spaced recall).

## Scope Decisions

| # | Proposal | Effort | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| 1 | Continue from last thought | S | ACCEPTED | Low risk, high UX value — mental bookmark |
| 2 | Quick-annotate gestures | M | DEFERRED | Gesture conflicts with PDFKit, needs base flow first |
| 3 | Highlight heatmap | S | ACCEPTED | Visual engagement map in page slider |
| 4 | Reading session recap | S | ACCEPTED | Warm session closure, reinforces annotation habit |
| 5 | AI "Best of" curation | S | ACCEPTED | Top-5 highlights in export, uses existing ClaudeAPIService |
| 6 | Argument map | L | DEFERRED | Complex UX experiment, v3 candidate |
| 7 | Action reminders in library | S | ACCEPTED | Badge on book cards, shows annotation progress |

## Accepted Scope (added to this plan)
- Continue from last thought (show last highlight when reopening book)
- Highlight heatmap (dots in page slider)
- Reading session recap (card on session end)
- AI "Best of" curation (top-5 starred in export)
- Action reminders in library (badge on book cards)

## Deferred to TODOS.md
- Quick-annotate gestures — needs gesture conflict research with PDFKit
- Argument map — complex UI + AI, needs UX prototyping first

## Key Architecture Decision
- Storage: JSON files in Application Support (annotations/{bookId}.json)
- PDF annotations continue for visual highlights, JSON for metadata layer

## Architecture

### System Diagram
```
ReaderView
├── PDFKitView (+custom highlight menu item)
├── ReaderModeView
├── ReaderToolbar (+📓 notebook button, +chapter banner)
│
├── ReaderViewModel
│   ├── annotationStore: AnnotationStore (NEW)
│   └── chapterReviewState
│
├── AnnotationService (existing — PDF-embedded highlights)
│   └── syncs with AnnotationStore
│
├── AnnotationStore (NEW — JSON-based)
│   └── BookAnnotations.json per book
│       ├── highlights[] (with reaction/action)
│       ├── chapterReviews[]
│       ├── smartHighlights[]
│       └── postReading
│
├── ClaudeAPIService (existing)
├── ChapterDetector (existing)
│
└── New Views
    ├── NotebookView (full-screen, replaces AnnotationListView)
    ├── AnnotationSheetView (bottom sheet)
    ├── ChapterReviewView
    └── PostReadingReviewView

LibraryView (+annotation badges)
ExportService (extended for Obsidian .md)
```

### Error Handling Requirements
1. Disk full on JSON save → catch, show alert
2. Corrupted JSON → log, start fresh with backup
3. AI malformed response → parse defensively, fall back gracefully
4. AI model refusal → detect, show friendly "AI unavailable" message
5. Concurrent writes → serial DispatchQueue with debounce (existing pattern)

### Existing Code Reuse
| Component | File | Action |
|-----------|------|--------|
| PDF highlights | AnnotationService.swift | Extend |
| Highlight list | AnnotationListView.swift | Evolve into NotebookView |
| Chapter detection | ChapterDetector.swift | Reuse for triggers |
| Claude API | ClaudeAPIService.swift | Reuse for all AI calls |
| API key storage | KeychainManager.swift | Reuse |
| Text extraction | TextExtractor.swift | Reuse for chapter text |
| Markdown export | ExportService.swift | Extend format |
| Book model | Book.swift | Add annotation count fields |

## Implementation Phases

### MVP (v1): ~2-3 hours CC
- AnnotationStore service (JSON read/write)
- BookHighlight model (avoid naming conflict with PDFAnnotation)
- Custom "Highlight" context menu item in PDFKitView
- Bottom sheet annotation (reaction 🎭 / action ⚡)
- NotebookView with filters (All / Actions / Reactions)
- Markdown export to Obsidian format
- Continue from last thought
- Highlight heatmap in page slider
- Reading session recap card
- Action badges in library

### v2: AI Deep Reading (~3-4 hours CC)
- Chapter Review: AI questions + feedback + summary
- Smart Highlights: AI sentence highlighting
- Post-Reading Review guided flow
- AI "Best of" curation in export
- Author formatting detection

### v3: Intelligence (~4-5 hours CC)
- Direct Obsidian vault write
- Cross-book connections
- Spaced repetition

## Eng Review Decisions (2026-04-03)

### Architecture decisions from eng review + Codex outside voice:
1. **JSON as sole source of truth** — no PDF annotations written to file. Highlights rendered as in-memory PDFAnnotation overlay.
2. **Store bounds (CGRect) in JSON** — text + page alone can't re-anchor highlights. Each BookHighlight stores array of line rects.
3. **Add summary fields to Book model** — `highlightCount` and `actionCount` in SwiftData for library badges (avoid JSON I/O per card).
4. **Both PDF + Reader Mode annotations in MVP** — BookHighlight has two anchor types: PDF bounds for PDF mode, character offsets for Reader Mode.
5. **Extend existing PDFKitView menu** — don't rebuild. PDFKitView already has "Highlight" menu item.
6. **One-time migration** — import existing PDF annotations into JSON on first load.
7. **Cleanup on delete** — delete JSON file when book is deleted.
8. **"Continue from last thought"** — defined as last created highlight (by createdAt). Save immediately.

## Design Review Decisions (2026-04-03)

### Interaction states (added)
| Feature | Loading | Empty | Error | Success |
|---|---|---|---|---|
| Highlight creation | — | — | "Не удалось сохранить" | Yellow fade-in 0.3s + haptic |
| Annotation sheet | — | Both fields empty (valid) | — | Sheet dismisses with spring |
| NotebookView | — | "Нет заметок. Выделите текст..." | — | Card list with filters |
| Export | "Подготовка..." spinner | Button disabled (0 highlights) | "Не удалось экспортировать" | Share Sheet |
| Chapter Review (v2) | Shimmer placeholders | "Нет вопросов для этой главы" | "AI недоступен" | Questions one at a time |
| Session recap | — | "Приятного чтения" (< 1 min) | — | "Сегодня: N хайлайтов, N ⚡" |

### Design decisions
1. **Tap on highlight → opens annotation sheet** — no separate "Annotate" button (subtraction)
2. **Color picker with 5 colors** — existing HighlightColor enum (yellow, green, blue, pink, orange)
3. **Soft fade-in animation** (0.3s) + haptic on highlight creation
4. **Chapter Review**: skip button + swipe back between questions
5. **Post-Reading trigger**: auto-detect at last 5% of pages + manual button

### Typography spec
| Element | Font | Size | Weight |
|---|---|---|---|
| Sheet: highlight preview | System | .callout | .regular, italic |
| Sheet: section labels | System | .caption | .medium |
| Sheet: text fields | System | .body | .regular |
| Notebook: highlight text | System | .subheadline | .regular |
| Notebook: metadata | System | .caption | .regular |
| Chapter Review: question | System | .title3 | .regular |
| Recap card: stats | System | .headline | .semibold |

### Color spec (using Theme system)
| Element | Source | Midnight value |
|---|---|---|
| Highlight overlay | Custom | Color.yellow.opacity(0.3) |
| Sheet background | Theme bg lighter | #1A1A1A |
| Sheet text fields | Even lighter | #222222 |
| "Готово" button | Theme tint | #FFF0D4 |
| Notebook card left border | Highlight color | per-color |
| AI feedback text | Theme tint muted | #FFF0D4 opacity 0.8 |

### Accessibility
- Dynamic Type: automatic via SwiftUI .font() modifiers
- VoiceOver: .accessibilityLabel on all buttons, filter chips, emoji labels
- Reduced Motion: respect UIAccessibility.isReduceMotionEnabled for animations
- Touch targets: 44pt minimum (SwiftUI default)

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 1 | CLEAR | 7 proposals, 5 accepted, 2 deferred |
| Outside Voice | `/codex review` | Independent 2nd opinion | 1 | issues_found | 3 tensions resolved (bounds, summary fields, Reader Mode) |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 2 | CLEAR | 1 issue, 1 critical gap (zoom alignment) |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | CLEAR | score: 4/10 → 8/10, 6 decisions |

**VERDICT:** CEO + ENG + DESIGN CLEARED — ready to implement.
